# Copy to inventory.ini and point at a fresh Rocky Linux 10 / RHEL-family host.
# 203.0.113.10 is a documentation address — replace it with your server.
[k8s_node]
node ansible_host=203.0.113.10 ansible_user=root
